# URL of the deployed Yelp proxy Cloudflare Worker (see worker/README.md).
# Leave unset to fall back to the built-in demo restaurant list.
VITE_YELP_PROXY_URL=https://prime-bites-yelp-proxy.<your-subdomain>.workers.dev
